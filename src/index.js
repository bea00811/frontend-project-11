// import onChange from 'on-change';
// import i18next from 'i18next';
import {
  run, watchedState, mystate, elements,
} from './init.js';

console.log('initnintinitntint');

run(watchedState, mystate, elements);
